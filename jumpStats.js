function JumpStats(wallHeight, jumpDistanceToWallAtPrepare, jumpDistanceToWallAtStart, jumpHeight, quality) {
    this._wallHeight = wallHeight;
    this._jumpDistanceToWallAtPrepare = jumpDistanceToWallAtPrepare;
    this._jumpDistanceAtStart = jumpDistanceToWallAtStart;
    this._jumpHeight = jumpHeight;
    this._quality = quality;
};

